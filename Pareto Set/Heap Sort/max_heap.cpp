#include <iostream>
using namespace std;

// Build heap -> TC O(n)
// Heapify -> TC O(logn)

void build_heap(int arr[], int n)
{
    for (int i = n / 2; i >= 0i --)
    {
        maxheapify(arr, n, i);
    }
}

int main()
{
}